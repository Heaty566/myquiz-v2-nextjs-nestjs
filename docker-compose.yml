version: "3"
services:
        server:
                build: ./server
                image: heaty566/myquiz-v2-server:latest
                ports:
                        - 4000:4000
                env_file:
                        - ./data/config/server.env
       redis:
                image: redis
                ports:
                        - 7000:6379
